[project]
name = "docvert"
version = "0.0.1"
description = "Layout-aware, validation-first workbook PDF to interactive HTML pipeline."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
docvert = "docvert.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
